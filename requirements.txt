# ============================================================
# CartComplete — Zomato Hackathon
# Full dependency list (CPU-only, ₹0 budget)
# ============================================================

# ── Core Data & ML ──────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
scipy>=1.11.0
pyarrow>=14.0.0

# ── Gradient Boosting Ranker ────────────────────────────────
lightgbm>=4.1.0

# ── Embeddings & Transformers (CPU) ─────────────────────────
sentence-transformers>=2.2.2
transformers>=4.35.0
tokenizers>=0.15.0
torch>=2.1.0,<2.4.0
--extra-index-url https://download.pytorch.org/whl/cpu

# ── Vector Similarity Search ───────────────────────────────
faiss-cpu>=1.7.4

# ── Graph & Vector Database ───────────────────────────────
networkx>=3.2.0
chromadb>=0.4.0

# ── LLM Integration (local / free-tier) ────────────────────
huggingface_hub>=0.19.0
ollama>=0.1.6

# ── API / Serving ──────────────────────────────────────────
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0

# ── Utilities ──────────────────────────────────────────────
python-dotenv>=1.0.0
tqdm>=4.66.0
joblib>=1.3.0
requests>=2.31.0
orjson>=3.9.0

# ── Testing & Dev ─────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.21.1
httpx>=0.25.0
